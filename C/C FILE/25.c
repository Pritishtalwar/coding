#include <stdio.h>
#include <string.h>
int main(){
    char str[100], str2[100];
    int choice;
    do{
        printf("\nMenu:");
        printf("\n1. Find String Length");
        printf("\nEnter your choice: ");
        scanf("%d", &choice);
        getchar();
        switch(choice) {
            
        }
    }
    return 0;
}